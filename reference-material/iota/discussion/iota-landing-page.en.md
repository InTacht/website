# Iota Landing Page

English translation of Hinglish discussion recordings (Sarvam).

## Chapter 1: Cortex-Compute

I told you that this is a mixture of experts. They will look at your logs and see if you first run the entire model on the cloud. If it's correct, they will continuously see what is repeating, which repetitive experts are calling out, and those repetitive experts who are not calling out, they will literally cut them down, quantify them, and do this. Your size has decreased, and because the size has decreased, your inference will also be quantified. This is one approach. Secondly, let's say our router is so well-contextualized that what can we do? If your query is an I-time series query, then your GLM, meaning the model fusion, this mixture of experts that you can use with different SLMs, that your router can be so real-time and fast. Hmm, what do experts do? They call out individual experts. From that, in SLM, 1, 2, 3, 4, let's say, for one, let's say, data bricks, SLM 150 million parameters 1. Google's time FM is for others. From that, what will these people do, where will they do the work, the case we are talking about. What is its name, Tanuj's Tanuj bhai's? You have to do this for the share market. You have tick by tick time series data. If you have to operate on that, then for that time series data, you will need Google's FM model. Plus, you need a good reasoning model. So, reasoning, yes, we are not directly putting the time series data tick by tick, right? We are putting it in the form of call text. Yes, what did I tell you in the cortex? Connect a data source. That is for the same thing, either you will upload a file or attach a live data source, or you can give an API endpoint. Understand? RAN AI, we are using it for the same thing, bro.

So that he can take that and do data fusion, and by doing data fusion, he can keep it like a semantic knowledge. Now, he will take it as and when it needs to be used, and from there, the API will immediately call in real time and finish the work. Understand? Okay? So, the same thing has come up, this call text here itself has become a data platform in a way, the call text itself. But technically, it is a data platform for AI. That's it. It's like a data platform for models, routing, and agents. Understood? That it's like a large amount of files that can upload everything. In that, let's say some real-time API or news, etc., need to be connected, they will do it, and real-time data will start coming in. Our backend will process it, and on that, they can do some transformation or run anything. So, they can run custom Python programs or whatever is needed. So, it will run inside Cortex itself, Cortex compute, you can say, right? That yes, Cortex computer. What is Cortex compute? It's like you have data, and you are running some custom query on it. Now, that custom let's say for the custom query, the same thing has happened that now you have Cortex, the way your program is, it involves some scripts, etc. Yes, custom computer and custom logic part has happened. That's it. Now, think of your custom script, if you normally write Python and go to process that data, it will take a lot of time. Versus you can write a similarly, like a very easy to do SQL command, okay? Data for that. That will run and come. Data DuckDB can write something, what will that come now? That will be your compute engines, different, inside Cortex compute. Understood. So, in one Cortex compute, like Cortex DuckDB, Cortex Polar, Cortex, so we will have specific sandboxes. Okay? There will be one Cortex Python. Which is a general Python one.

So, you're just executing some Python code, right? That kind of thing. So, that will come inside the Cortex compute, sandbox. Explain it. Understand, but when is it beneficial? Think about it. What about agents? Look, what are people doing now? People are giving Linux machines to agents, giving VPS, giving this and that. But the problem is there, right? So, give a VPS machine, VBS machine, every time, let's say you take a sandbox. So now, in a similar query, let's say you need the same Docker that's currently running in every sandbox. The same, yes, you have to run Docker too. The same, you'll have to download your file too. So, every time, let's say you download, upload, download 5, 6, 8 GB worth of Docker images, half the time will be spent processing that itself. Because you understand, right? You're not getting the right kind of results in real time. And Cortex speeds up the same thing. In real time. And because of the sandbox, whatever downloads you're using, there will be some issue, meaning it will take time. Because of that, the sandbox isn't efficient either. This thing, once we figure it out correctly with a router, we can fit literally efficient sandboxes. Hmm, that's it. And then they say the router is done, and then this call text comes. Now, look at in-place editing in call text. In in-place editing, no one is giving it right now. Okay? Like, the person who was in YC told us that you need to get the dogs edited. Okay? Now, look, he also created the same thing, told us that if you want to edit a dog, how do you know how to edit the model, like this and that in this line? So, he created an MCP kind of thing of the same thing, that if you have a dog, you can edit line by line with precision, particular dog. Right? Or do anything. So, he was bringing different file types and their support from that.

I'm understanding a guy. Yeah, understand. Right, bro, that's the thing. Cortex is mainly going to be your main game. Understood? But we can't make Cortex completely sci-fi and high-fi. Understood? But yeah. Now we have to go step by step. We have to go step by step. We have to go.

## Chapter 2: File-System

Now, you have to do a static analysis of the binary file. How would you do it? For that, sir, we have this. Hmm. Okay, let's give it a try. This is the file system, right? Do you understand? Especially in multi-model, the thing is, the way multi-model systems are coming up. All the things, routing, and you can't rely on models for this, you can rely on them. As for the Cortex, in Cortex too, now you have to think about, like, let's say this is my cloud, or however I am editing the code, I am doing that, so it's my files, right? You are editing the files and all that, and you are not doing anything else. So, technically, what happened is that you basically created an application like DeepSeek Harness, in which there is a folder, and all the files inside the folder, all those files, that's your context, first thing. Okay, in the case of code, and when you edit something, whatever you do, those edits have to go into the same file, right? You are not generating anything new as such. Right? You have a motivation, a goal, you chat, you do that. So, technically, your context, your cortex, that cortex has become this. The data that happens inside the folder. Hmm, yes, you understand, your call tax has become. This is how people can do it, right? So, it depends on the application, the application use case, what a cortex is considering. Understood? So, we are creating it for that angle, thinking that let's say you generated a cortex. Now, in the cortex, as I said, let's say Fincoach has to create a model like this, okay? For example, for a human, right? Yes. They want to build such a model where that thing is happening. Now, think about what the parent cortex will be in this. How did the data in the parent cortex come? It's coming through their research team, right? Parent cortex.

So, that data is something that has to go with every query you have. It's like something around which you can search. And what all is there in the cortex? I'm running the cortex as a combined system of new age MCP, connectors, and API calling, tool calling. Let's say you need some data from MCP for a model. Hmm, okay? But why do I have to call the MCP protocol? Sanjay, understand? I can simply tell Cortex to give me this data, it will come and finish. Hmm. Okay, I need to get it computed. I wrote a Python script, I told Cortex, Cortex do this for me. So, it will give me the final result on my data. Okay? I told Cortex that I have files, upload them to that Cortex. So, it will upload them. It's technically a cloud-based file system, right? Okay? It will upload them. Now, let's say I told Cortex to change this file script or whatever it is, it will be done in the computer. You, hmm. Yes, hmm, that's how it was, now what are the end-use cases we have that we can create for now? Jump, no. The most important thing is done. That's done. No, oh, platform. That's left, now it will create this, it can also be sold, yes, it can be sold, but the thing is, let's say we have created this cortex, where can the use cases be? Hmm, Phinziga. For now, these are the 2 use cases we have. And that deep C garnish. Iota. We will release that as an alternative to our Grok bot, right? That's it.

## Chapter 3: Memory stitching

Okay? So, it's happening when the user goes into the app and turns the MCP on and off. Right? Because your data is coming from that. So, that thing will use it as a tool call, but it won't show the tool call to the user. So, it will use it internally on the backend. So, that loop of yours will be such that you'll give it to the model, configure an MCP client with the model, which will be running on your backend at that time in the icon. Okay? It's like your backend itself is now an MCP client. That you registered the MCP, the model did something, the model returned the tool call, they called it back, ran it and showed it. Okay, whatever responses the tool asked for, you ran the whole loop, like it was going back and forth. You kept doing it, whatever answer came, you gave it. Hmm Hmm Hmm like Right hmm, so you'll build it like this, right? Hmm, that's the thing. Now, thinking about it, what tools are available to you right now to do this? You can do it in Biofrost, that's all. You can go to Biofrost, go to MCP, and configure it. Okay? So, then you can use that MCP. While it's calling that API. Understand, are you getting it? So, that's what's missing, right? So, think about it, along with the router, let's say you want to combine your data with the model. And so, what do you need, that you do data stitching, context stitching somewhere else? Why do you need to do it? That's the thing. That's what this product solves, right? Okay? That with the model, there's a context of the session. Whatever context is coming with the chat you're having, bring it. Bring it. Like this session was there, a new agent started, a new contract started. A new cortex started. Okay? So, that's a different thing. Okay? You won't put that in your memory. And that thing, that won't even be stored in your cortex.

That's not something that gets stored in your cortex. But the thing is, I'm currently working on an uncarbonized code base. Every time I start a new agent, the context is different in the new agent, it's not the same. The same thing has to be used at that level. Yeah, so you understand the difference, so understand. That harness, that harness-level context. Global hmm. What context does the Open AI model store? It doesn't, right? It's giving you a 1 million context window. Now, in that window, as you add new context, you saw that there was an error just now that there's a limit of 200, so you can send 238 or even 201, right? But it's not going away. That API won't allow you to do that limit. Hmm, you understand, that's what the harness overcomes, right? What does the harness do for you? It helps you with whatever limit you have for calling the API, right? Hmm, Vajir is done. Now you have a limit of 200 on the model. But you have a context of 10 million. So what will you do? You'll break it down into 5 different parts, right? 5 parts, and your 1 million context is created. Now what will you do next? You'll break it down again. So it's 1 million, then that's it, you'll do it like that, right? Summarizing the context. Understood. Contact hmm, it will work that way. That context management is something else. That's part of your harness, it's not someone else's deal. Something? So RLM has given a solution for this. Hmm, what did RLM do? It's able to scale those models that could only handle 100 to 1 million contexts to 10 million or 20 million contexts. Because they figured out a loop that's not a harness. A harness means everything is open-ended tools, they're doing everything, they're able to do everything. Here, that's not there.

Here, there's a fixed thing, but it's fixed in such a way that you can do multiple context memory stitching and all that. Do you understand here? Cortex is our same thing. What is Cortex is, let's say when you are not able to fine tune the model, right? When you have the model, now you tell me one thing, if any data is changing in real time. If any data is changing in real time, like news. News is such a thing that I can fine tune it, right? No, absolutely not. News and something that is constantly changing. Okay? So when I have a query, if News is asked in the query, then a tool of mine will call and immediately fetch the news. Okay? But if I am dependent on Google or dependent on somewhere else for fetching that news, right? Then my time will be wasted, right? There will be a bill for the same thing, so Cortex is the solution to that. What does Cortex do? It's an add-on memory on top of your existing model calls that you are going to do. Okay? This add-on memory is something that provides you causal intelligence. Causal means changing or evolutionary intelligence. We are calling it Cortex for this thing. Hmm, explain. Okay? What is happening in Cortex is that that data is going to be stored. Which data is it? It's not the user's data. It's the data that you, as a developer, because you started this project. You configured that router, right? You are taking the API of that router, right? You will configure Cortex. Now, what can I do in Cortex? Let's say I have a Postgres database. I will directly connect it. Okay? So now that database of mine is connected there. So now, when I query it directly, Cortex knows everything about my schema and my data. Huh, okay.

? Now, I don't want to think about what's different and what's not, all that. That cortex is that part. More and less, it's the same thing we were making a year ago, kind of the same thing. Oncology, you know, the same thing. A little similar to this. That's not technically ontology. Yeah, I mean, ontology also has an evolved version, an evolved version, that literally LLM is a center of it. So, how can you do this at least in it? Huh, bro, now what's happening with RLM is, when you combine it, how will there be parts in RLM? The RLM that's processing the query, the user's context is also coming along with it. Understood? What did the user do that they had to send a chat, they'll send a session, right, the API with completions, okay? What will they do inside that, they'll start a session, in which session they'll just start sending query after query, data after data, put in prompts, okay? All the context that comes, you'll get it all in the form of prompts. Okay? Now, that's up to us. That if we're using which one, we're making it Open AI or we're following Anthropic's format, it depends on that how we're taking the images and all. Right? How are we going to process the attachments. Understood? So, if I have to process the attachments, I'll have to create my own custom API in a way. Where you'll do, along with the prompt, you'll also send the attachment. I don't think so. Understand. Okay. So, it'll be like that. And the second thing, the attachment that you gave me, where will that attachment go? The main thing is that if it's a Temporary call tax. Temporary texture. The simplest. Look, let's say if you're making an AI app, okay? To make that AI app, you need to, what we saw just now, drag and drop the file, JPG, it's gone, it's gone. Make down file, it's not working, right, mark down file. It doesn't have support.

Malcolm Yes, so even if you want to bring that, you still have to write the code, you have to bring something new. You have to bring something new. MD file is much easier than PDF, and it didn't support PNG JPG. It still didn't. So, what's the cortex doing now? Your chat box, you have to send data with the chat box. Your cortex is doing that work, right? Hmm, understood? White Cheese, I mean, I mean, think like this, think like API, what would be API like? Let's say they say api.intech.com/cortex/new. Okay? As soon as I did new, I got an ID. Okay? On that ID, now like the user has put in 4-5 files, put in a video, whatever needs to be put in, I have taken everything. Getting it? Whatever he put in, I have taken the whole context in one go. Okay? And now what have I done, I have also taken the prompt of the user. Okay? Now what can I do when I am sending the prompt, so what will I do with the prompt, think. That I will say that whatever the user's prompt is, let's say analyze these files or whatever the prompt is, I will write a call text ID below it. Yes. So, what will happen, my gateway, my router will understand that the text prompt has come, but some files have also come with it. Where are the files? Understood? Those files have already been uploaded to my Cortex. Because I took the session ID, the file that the user uploaded, I uploaded it to that ID. Okay? As soon as I upload, my Cortex program will process each file according to its file type. And this is the context it will create and keep ready for model processing. For model processing. This is the way context will work only once for a durable workflow. That's it. Because the context has been saved. Otherwise, if the chat session is gone, then you are also gone.

So, the forest, the stone, and the seed, I mean, yes, but because of this, if the context ID is sent every time in this way, that ID will be there, the ID means that time frame, etc., will be done accordingly. According to that, then we can plan durable workflows, plan cloud agents. So, right now you are running locally. By running locally, let's say you want to shift the workload to the cloud, you want to get this done, this part of it. Here, right, this is the future for us. Okay. So, cortex is like a temporary storage, where you are sending data, you can store some state for durability, you can store whatever you need. Cortex is like an object storage in a way. Object Storage is the word for object storage, but for AI. Is that right? So, it will upload the video, that will also be stored, and + frame by frame, like in the spiral, what was there in the spiral DB? There was a video here, you can do frame by frame analysis of the video you sent, but how is that video being stored? It's being done frame by frame. It's being done frame by frame. And it's happening with your tagging along with the frame. It can happen with your tagging along with it. Yes, tagging can happen, that's the thing. That's the thing. So, you are designing cortex in that way, getting it? That what is in cortex is that, right now here, we were attaching files. Cortex is all about end Context is all about that you will give a file and plugin inside. But Context is kind of smart indexing, you can say kind of. Smart indexing, which you can also retrieve from this future LLM, and it can also be saved as a log because in the future you can also help in fine tuning it. Okay. What is it in the current case is that, like 1, so cortex will be mine, which I want to store permanently, correct? Which is my fixed cortex. Fixed cortex, like what happened?

Like, I have a database where some things keep increasing, they keep happening, and I don't want my users to mess up that cortex. So, that's my one fixed location of the cortex. Then, I'll create temporary cortices. What are temporary cortices for? I need to handle a particular user's session or a particular user's chat. Okay? So, think about it there, like if you run a chat session on the cursor, the context in it is only for that time, right? After that, it's gone, Hmm, so Hmm. Okay, okay. After that, when you start another agent, it's not in that context anymore, right? Hmm, cortex is the same thing. That's it. In a different way, temporary object raw data, whatever you consider it to be. Are you getting it? It's like, if you go to an object store on S3 and create an S3 file, right? What happens with that is, you get an S3 path. Now, you get a bucket, an S3 bucket. Now, you put any data, anything, in the S3 bucket, it's your store. Okay? Now, if you want to retrieve it, it has different APIs. Yes, that's it. It's like that's the best thing, and on top of that, using smart routers, you can use different models and do this and that. That's it. And we've kept Cortex in such a way that it will look like a file system to the end user. You upload a file, and you see a list of files. Okay? You create a folder and upload a file, and it shows up. You put a zip file, and the zip shows up. Temporary file system, this is it. Temporary file system, which is easily accessible for AI, and this is the way. Yes. And when you are putting any data in the cortex, we have a file processing agent that is doing it. The same thing is happening with the cortex. Bile processing is happening. The same thing is happening with context. One thing is that you can do smart routing. Caching. The caching part is done. There's a tremendous amount of caching context. The window is expanding.

They're doing caching airport. It's like that, right? This is like real-time rag kind of thing, right? Yeah, now when you create a cortex, think about it, let's say you need to support connectors. How will you do that? For connector support, you'll take a separate code and bring it. You'll fetch data from Google Drive. How will you do that? Mm-hmm It's very difficult, okay. What you simply do here is add files from Drive, okay. In the UI, you'll click there, and a link will be created with your Call Text ID, and your data will automatically sync with the Call Text. Okay, you understand, your drives. Okay? Bring the files you need to bring. Okay? Drag and drop or whatever you need to do, do it. Okay? So Cortex fixes that for you, right? It will see your connectors as a file system. Understood? Now, let's say you have a database, you need to give the database directly, okay. And the big thing. Big database, more database. You need to give a database, it's a straightforward thing. I said, let's say I have a Postgres database with a public URL. Okay? Now, what did I do with that public URL? I created a key, created a user account, and directly dropped the URL here. Meaning, when I'm doing that, the attach button, I'll have options in attach. Upload a file, okay? Connect with Google Drive or Connect a DB. You understand. Correct Hmm, so suppose I need to create a connector DB function. I'm saying this from the perspective of an app, from the perspective of a use case, that let's say there's someone who wants to create an app that has this. That it needs to take data from the database. Okay. And this is only valid in that chat, okay?

? In that chat, as long as he keeps chatting, that context will be used, and as soon as he closes or deletes the chat, the session will end. Okay, understood? All the connections he made will also be gone. Hmm. Understood? Hmm, so that will also be gone. Understand? And plus, the trajectory that he was creating here, deep seek. That trajectory is going directly into the cortex. So, in a way, a log of the chat level or session level is also being stored. Hmm, a block is also being stored in it. That's the thing. So, you can see from the past call that you tried this thing in the past. But it didn't work because of that. So, you can search for that and this too. Let's say there are 3-4 ways to do one thing. This 5 command, let's say your machine's configuration is such that it doesn't run after running 2-3 commands. It's running on the 4th command. So, it will keep that context in the cortex that, "Hey, its configuration is like this." So, for this, what will have to be done? The step of execution, it's not 3-4, it's 2-3-4, execute like that. Hmm, that will be the thing. Yesterday, like we saw, we started H-stop, but the server wasn't connected for H-stop. The command to connect the server is done. On that too, let's say you've built a Docker, so you have to run the Docker build first, right? You haven't installed MariaDB locally and run it. So, for the Docker build, you have to do this for the Docker build. So, it will be stored in that session in this way. This thing, hmm, this thing, it's more like a file system. Now, in that, whether you are an agent, a harness, or a model, whatever it is, first of all, the model will never directly store anything. Okay? The model is just a reasoning provider, a token generator. Okay? It's just a token generator, it will tell its reasoning in that particular response.

That's it. Action calling, etc., is mostly done by either a tool calling person. Okay? Okay, hmm. And no one else will do it. Hmm. That's the simple thing. Understood? Hmm. So, that's what we need to do for this. Think of it this way: the scope of a router is so much. I mean, the router is growing in such a way that it's becoming a file system and all. Not a router, it's Cortex. Cortex. Okay? Now, think about what I told you. Cortex helps you define sessions. This cortex helps you create durable sessions. Cortex is for the durability part, think of it that way. To see what things are going into your context or session, to keep them in a visual form, to keep them in a way, right? Cortex is for that. Okay? Now, what can you do? Let's say you're creating something with default Cortexes. What can you do? You can create a parent cortex or a default cortex and attach it to whatever API key you're creating. The one you call, like your system prompt. Think of it like a system cortex. So, in a system cortex, whatever data or context they've given to the model level, like global knowledge. Okay? So, that global knowledge will be with the system cortex. So, the system cortex, hmm, okay, think of it like that, it will be with it. Now, when the user starts using it, what will happen at the user level? Everyone will have a separate session. So, what will happen for the session? A new Cortex ID will be created. So, a Cortex ID will be created. Your data and everything else will be stored in that Cortex ID. Now, you'll send that Cortex ID in every chat with a prompt. Whatever prompt you're sending, the chat ID will go inside it. That's what will be right. That's what will happen. This hybrid thing. This is also a kind of, I mean, the same thing, I mean, we can use this kind of use case in it. Local to global, let's say.

So, a computer that's limited to local models. For this, the context cortex will be important to us. Like, let's say you go to an orchestration. You see, a new tab has been added to the orchestration, a local model plus Fabel 5, a combination of both. So, in this smart orchestration, you can do this too. Let's say you have a Pebble 5. So, select the model. That these are the API keys I have, 4 API keys. The router will correctly figure out what to give to whom. Run this on the local model with WaveWolf 5, and stitch it together in this way. So, a combination of both. That sensitive data stage local. This will happen because of this. Smart Orchestration, or rather, our so-called Cortex. Cortex means the same thing, right? Yeah. Now, the sensitive data you took, that sensitive data is going into your Cortex. Okay? So, even if you turn off your laptop tomorrow, your data will still be there. But we are not sending that data to the actual model provider. If, let's say, you have to implement something, like Pi classification, etc., with personal identifiers. Like, if there is a personal identifier inside it, then in that case, what is happening is that data is processed only on your system. Right? Yeah, but listen. So, that's for that thing. Yeah. That's the thing. Hmm, so overall, this is also to be known, meaning along with this. Yeah, The GLM 5.3 flash uncensored. This is visible.

## Chapter 4: Reasoning and context differentiation

The system that can solve the Red Kill Red Kill problem. Right now, there's just one thing missing. Tell me, have they solved the context problem? Have they solved the navigation? They've solved the navigation to some extent, but with the navigation, the thing we discussed yesterday about the cortex, right, that the router itself should know how to take memory, how to access memory? Yes, so what's the point of that? We're actually removing the context problem from the actual language model, right? That the actual language model's job will only be reasoning. And he says the same thing in the video in the first few seconds, that we are actually separating reasoning from the actual storage of the model, and its standard method is RAG, as of today, that you put memory and storage in RAG, and whenever a query comes, bring the chunks that match the query and send them to the prompt. But you make the model do the reasoning. He's making the model do it. Just optimize that process. You can dramatically reduce the cost. He did it with Coin, you do it with Coin too. The Coin that he used, the model, that 3 billion one will run smoothly on your laptop. 3.5 Coen 3.8 will also run, see. Okay. And Quen, that one will also work, what's its name? Then see, what's our main thing because look, the Liquid that launched, the Liquid FM models that were there, the Liquid AI ones. So those models are quite fine-tuned. They're so fine-tuned that even in very small, I mean, parameter size, they're giving you reasoning capability, thinking capability. Do you understand? So if you do that, because look, the contact size is less because the parameter is less. Okay, the reasoning isn't that efficient, I mean, it's not generalized. So if you use all its reasoning capability and supply the memory capability separately.

So, he can do good work. You can handle many queries on the simple on device SLM. Most of the queries. Most of the queries. And the queries that are getting a slightly wrong answer, that part of that will be part of your training program, that you will decide those queries, okay, this answer is not good. Like, he will give the answer like this, and not like this, dislike the answer. So, if you like that answer, okay? Or someone says, change the variant, show me a different variant. So, when he is doing that, then in that case, the router should know that he has to use a high-quality model now. So that if that answer comes and if the user likes that answer, then for the next time, that will become your trace. Yes, do this with the traces. By using those traces, he will regularly train his SLM. This is the self-learning routing system. And from this, organization-specific autonomous, so-called fine-tuning will start, meaning. That's it.
